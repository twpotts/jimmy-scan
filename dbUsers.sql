-- Create table

-- CREATE TABLE user (
-- 	id INTEGER NOT NULL, 
-- 	username VARCHAR(20) NOT NULL, 
-- 	password VARCHAR(80) NOT NULL, 
-- 	PRIMARY KEY (id), 
-- 	UNIQUE (username)
-- )

-- Add user

-- INSERT INTO user 
--     (id, username, password)
-- VALUES 
--     (3, 'tyler2', 'potts2');

-- Delete user

-- DELETE FROM user
-- WHERE username = 'tyler2';

-- Uncomment section desired section
-- Click on View > Command Palette > SQLite: Run Query
-- Right click on database.db > Open database
-- In SQLite Explorer on bottom left, expand database.db
-- Right click on user > Show Table